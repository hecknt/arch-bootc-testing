# The swtpm package adds a swtpm.conf file to tmpfiles.d, but its function is ONLY to change the permission node of the directory /var/lib/swtpm-localca. It does not create the directory.
d /var/lib/swtpm-localca 0750 tss root -
